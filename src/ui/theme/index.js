import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
  primary: { main: '#00695C' },
  secondary: { main: '#6A1B9A' }
};
const ThemeName = 'Tropical Rain Forest Seance Sablefish';

export default createMuiTheme({ palette, ThemeName });

